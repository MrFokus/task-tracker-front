import { io } from "socket.io-client"

export default defineNuxtPlugin(() => {
    // const socket = io(useRuntimeConfig().public.baseUrl)

    // return {
    //     provide: {
    //         io: socket
    //     }
    // }
 })